# 菲比主题、触摸响应与无线会话修复

## 目标

在保留 Chibi Skadi 主题和现有功能的前提下，解决 Tab5 会话详情返回失效、按键偶发无响应和界面卡顿，并为现有 `feibi` Pet 增加一套独立 Q 版主题。会话详情继续支持 USB、Wi‑Fi 与 BLE，不把“需要电脑 Bridge”误写成“必须连接 USB”。

## 已确认根因

1. 详情页返回键只有 92×46 像素，并等待释放事件才执行；主循环被动画帧或大区域提交阻塞时，释放事件容易丢失。
2. 宠物切换后使用全局 300ms 触摸锁，锁定期间其他区域的新触摸也被吞掉。
3. 自定义 Pet 每次动画换帧都会重新解析 active manifest、打开 microSD 文件并读取 319,488 Bytes；约 8 FPS 时持续占用主循环。
4. 详情滚动会重绘并提交包含标题的 808×608 区域，而标题在滚动时并未变化。
5. `thread.open` 与 `thread.detail` 已是 BLE 可用的可靠 v4 消息，单条限制 8 KiB；当前自动化也已使用 BLE 传输验证详情。电脑 Bridge 仍是 Codex 数据源，因此“无线”表示不需要数据线，不表示电脑可以关机。

## 修复方案

### 触摸

- 返回键扩大逻辑命中区域，并在按下边沿立即关闭详情，不等待抬手。
- 宠物切换键在按下边沿立即执行，持续按压只执行一次；释放隔离只拦截同一切换键，不再全局吞触摸。
- 拍照键在按下边沿反馈并执行一次。
- 保留审批按钮的按下/抬起安全确认，不改变高风险操作语义。

### 性能

- 在 PSRAM 中缓存当前动画行的 8 个 RGB565 帧；同一动画循环不再反复读取 microSD。
- Pet 或动画行变化时才执行一次连续行读取，读取失败时回退到单帧或矢量 Pet。
- 详情滚动只重绘并提交消息视口和滚动条，不重复提交标题与返回按钮。
- 触摸活动期间暂停宠物动画提交，优先处理输入。

### 菲比主题

- `feibi` 启用深莓紫、奶油白、香槟金、紫罗兰与玫瑰粉配色。
- 背景使用星光、缎带和圆润装饰；状态、连接、电量、额度、Token、按钮和滚动条使用菲比专属图形。
- 项目使用金色缎带/文件夹卡片，纯对话使用紫罗兰圆角气泡卡片。
- 电脑控制页与 Tab5 同步启用，不影响 Skadi 或默认主题。
- 菲比仍作为 microSD 自定义 Pet 安装；本轮通过已连接 USB 预装到设备，后续切换无需再次传输。

## 版本与验收

- Bridge 与固件功能版本升级为 `0.4.0`，协议继续使用 v4，保留现有配对凭据。
- 原生测试覆盖触摸隔离与 Pet 选择守卫；设备协议测试明确保留 BLE 会话详情。
- 浏览器分别验收菲比主页、项目详情、纯对话详情。
- 完整 JavaScript、固件核心、真实 ESP32-P4 构建通过后写入当前 Tab5。
- 真机回报 0.4.0、USB 在线、SD 就绪；随后预装菲比并恢复用户原先选择。
