# 滚动性能、Pet 节奏与 Skadi 称呼修复

## 目标

- Tab5 任务列表与会话详情在跟手滚动时不再整块重传右侧视口。
- 所有 Pet 动画保持动作可读，但降低重复循环和眨眼频率。
- Skadi 主题的界面和 LLM 对话统一称呼用户为“博士”。

## 实现

- 将滚动刷新间隔从 33 ms 调整为 16 ms。
- 每一帧先在 Tab5 显示缓冲区原地移动已有内容，再只从主画布补绘新暴露的顶部或底部条带以及滚动条。
- 任务列表和会话详情共用同一增量滚动提交路径；大跨度跳转仍回退为完整视口提交。
- 所有动画循环不少于 2.2 秒，Idle 在眨眼动作前后各停留 2.5 秒。
- Skadi 主题的用户标签改为 `YOU // 博士`；创建 Skadi 对话线程时加入“始终称呼博士”的角色约束，切换 Pet 后新建对应角色线程。

## 验收

- 固件核心测试覆盖动画最短循环和 Idle 停留时间。
- 固件静态检查要求 16 ms 刷新、显示缓冲区原地滚动和暴露条带补绘。
- Pet Agent 测试验证 Skadi 使用“博士”，并在 Pet 切换后创建新的角色线程。
- `npm run check` 通过，共 159 项测试及固件核心检查。
- 真实 ESP32-P4 发布构建通过：RAM 49,568 / 512,000 Bytes（9.7%），应用 6,372,108 / 6,553,600 Bytes（97.2%）。
- v0.4.2 整机镜像为 16,711,680 Bytes，SHA-256 为 `502e2e6ca220baf9a1a4a398eb4efa4edd14e86741c5423af724f566354a8399`。
- 非擦除真机升级完成，bootloader、分区表、应用、离线中文语音和 SPIFFS 均通过写后哈希校验。
- 只读真机验收通过：固件 0.4.2、协议 v4、USB+BLE、microSD、离线中文语音、4 个 Pet、12 条最新任务和 Token 数据均正常。
- 滚动手感和动画节奏仍需用户在屏幕上目视确认；自动化不能代替物理帧率与主观手感验收。
