# 主动关怀与多轮陪伴升级执行计划

## 一、最终目标

在不引入 OpenAI API Key 的前提下，基于项目现有的 Codex App Server、Tab5 摄像头、麦克风、离线中文 TTS 和加密设备协议，实现一个可长期运行的个人主动关怀系统。

最终行为必须同时满足：

1. Bridge 可在设备空闲时随机、周期性要求 Tab5 拍照。
2. 同一个 AI 会话能连续接收摄像头画面、Codex 状态、历史记忆和用户语音。
3. AI 可选择保持安静，也可主动开口询问用户状态。
4. AI 开口后，设备在朗读结束时自动进入聆听，不要求用户每轮按住按钮。
5. 用户和 AI 可以在同一上下文中进行多轮对话，直到任一方自然结束。
6. AI 可通过严格白名单动作调节 Tab5、打开预设应用或媒体、安排复查，并在执行后继续对话。
7. 不设置“关怀后固定冷却 6 小时”；观察与跟进时间由当前状态动态决定。
8. 只保留约 90 秒的技术防重复，防止同一事件、重复包或重连导致连续触发。
9. 电脑仍使用现有 Codex 登录状态，不要求 `OPENAI_API_KEY`。
10. Codex 代码任务和陪伴会话相互隔离；陪伴线程只读、禁止任意工具和工作区修改。

## 二、已经确认的技术选择

### 2.1 AI 接入

继续使用现有 `CodexBridge` 连接 Codex App Server：

- `thread/start`：创建陪伴会话。
- `turn/start`：发送文字、状态摘要和摄像头图片。
- `LocalWhisperTranscriber`：把 Tab5 的 16 kHz 单声道 PCM 汇成临时 WAV，在电脑本地转写并立即删除。
- `turn/start`：将本地转写文字送回原有陪伴会话，得到下一轮回答。

不接入 OpenAI Platform API，不增加 API Key 配置，不引入第二套账户或计费链路。

### 2.2 对话形态

第一版使用半双工对话：

1. AI 返回文本。
2. Tab5 使用现有 ESP-SR 离线中文 TTS 朗读。
3. 扬声器播放完毕后，自动打开麦克风。
4. 用户说完后自动停止录音并上传。
5. 同一个陪伴线程生成下一轮回答。

原因是当前 Tab5 实现中麦克风和扬声器不能同时工作。第一版不增加云端语音下行和音频播放协议。

### 2.3 AI 输出合同

陪伴线程的每轮最终输出必须是可校验 JSON，而不是从自然语言中猜测动作：

```json
{
  "say": "你看起来有点累，是代码卡住了吗？",
  "continueListening": true,
  "nextObservationMinutes": 8,
  "action": null,
  "memory": {
    "summary": "用户正在排查一个持续较久的 Bug",
    "importance": 2
  }
}
```

动作示例：

```json
{
  "say": "好，我帮你打开网易云。还想继续聊这个 Bug 吗？",
  "continueListening": true,
  "nextObservationMinutes": 15,
  "action": {
    "name": "open_app",
    "arguments": {
      "presetId": "netease-music"
    }
  },
  "memory": null
}
```

Bridge 必须验证字段、类型、范围、动作名和参数。解析失败时不得执行动作，只返回一个安全的普通回复或保持安静。

### 2.4 摄像头调度

默认策略：

- 普通空闲：随机 10～30 分钟观察一次。
- 上一轮不确定：3～8 分钟后复查。
- 正在对话、录音、朗读或传图：暂停新的定时观察。
- AI 可通过动作要求立即再拍一张。
- AI 可在每轮返回下一次观察分钟数，允许范围为 1～120 分钟。
- 同一设备 90 秒内不重复启动新的主动观察。
- BLE 链路不传图片；只有可用的 USB 或 Wi-Fi 会话才触发拍照。

### 2.5 本地记忆

记忆分为两层：

- `care-profile.json`：稳定偏好、AI 人设、常用应用、当前摘要和最近会话信息，使用原子写入。
- `care-events.jsonl`：观察、开场、用户回复、动作和结果的有界事件流。

默认保留最近 30 天或 5,000 条事件，达到任一上限时压缩。摄像头 JPEG 继续在分析后删除，PCM 不写入磁盘；这是为了控制长期运行的磁盘占用和故障面。

## 三、阶段与提交纪律

每个阶段必须：

1. 只修改本阶段列出的范围。
2. 先运行本阶段最相关的测试。
3. 验收条件全部满足后创建一次中文 commit。
4. commit 后确认工作区干净，再进入下一阶段。
5. 不修改或弱化已有测试来迁就实现。

除本计划阶段外，预定共有八个实现阶段。若实现中发现必须改变公共协议或阶段边界，应先更新本计划并单独说明原因，不能静默扩大范围。

---

## 阶段 0：固化执行计划

### 目标

把用户已经确认的产品边界、架构、阶段顺序、测试和完成条件写入仓库，作为后续实施合同。

### 文件

- 新增 `2026-07-30_001.md`。

### 验证

```bash
git diff --check
git status --short
```

### 验收条件

- 文件名符合 `YYYY-MM-DD_XXX.md`。
- 明确不使用 OpenAI API Key。
- 明确摄像头主动观察、无固定 6 小时冷却和多轮对话。
- 每个后续阶段都有文件落点、验证命令和中文 commit。

### 中文 commit

`制定主动关怀多轮对话实施计划`

---

## 阶段 1：建立关怀状态、配置与长期记忆

### 目标

先建立不依赖 AI 和硬件的领域底座，使关怀状态可以持久化、重启恢复、进入 Bridge 快照并被测试。

### 具体工作

1. 扩展 `SettingsRepository`，加入有界、可校验的 `care` 配置：
   - `enabled`
   - `observationMinimumMinutes`
   - `observationMaximumMinutes`
   - `autoListenSeconds`
   - `duplicateGuardSeconds`
   - `persona`
   - `allowedActions`
   - `appPresets`
   - `mediaPresets`
2. 新增 `CareMemoryRepository`：
   - 原子加载和保存 `care-profile.json`。
   - 追加 `care-events.jsonl`。
   - 过滤损坏行并报告诊断。
   - 按天数和数量压缩。
3. 在 `DeskStore` 增加 `care` 状态：
   - `idle`
   - `observing`
   - `thinking`
   - `speaking`
   - `listening`
   - `acting`
   - `failed`
4. 快照只暴露 UI 和设备需要的摘要，不携带完整历史。
5. 在启动与关闭流程中加载、刷新和关闭记忆仓库。

### 预计文件

- 新增 `src/server/care-memory-repository.js`
- 新增 `test/care-memory-repository.test.js`
- 修改 `src/server/settings-repository.js`
- 修改 `src/server/desk-store.js`
- 修改 `src/server/index.js`
- 修改相关设置与 Store 测试

### 验证

```bash
node --test test/care-memory-repository.test.js test/desk-store.test.js
npm run check
```

### 验收条件

- 重启后配置、摘要和事件历史可恢复。
- 损坏文件不会被静默覆盖。
- 事件超过上限后可以安全压缩。
- Store 快照中的关怀字段有明确大小上限。
- 不新增第三方依赖。

### 中文 commit

`建立主动关怀状态与长期记忆`

---

## 阶段 2：统一 Codex 多模态陪伴会话

### 目标

让文字、摄像头观察和之后的语音转写进入同一个 Codex 会话，形成真正连续的上下文。

### 具体工作

1. 从 `PetAgent` 中抽出可复用的 Codex 回合管理器：
   - 创建只读线程。
   - 启动回合。
   - 汇总流式回复。
   - 超时、失败和关闭处理。
2. `PetAgent` 继续使用该管理器，保持原有对话和命令行为不变。
3. 新增 `CareAgent`：
   - 持有一个长期复用的只读、`approvalPolicy: never` 陪伴线程。
   - 启动时注入本地记忆摘要和当前人设。
   - 支持文字输入、图片输入和状态上下文。
   - 校验 AI JSON 输出。
   - 输出 `say`、`continueListening`、`nextObservationMinutes`、`action` 和记忆更新。
4. 摄像头观察可以返回 `say=""`，表示观察后保持安静。
5. 会话异常或 Bridge 重连后，使用记忆摘要创建新线程继续，而不是丢失全部上下文。

### 预计文件

- 新增 `src/server/codex-conversation.js`
- 新增 `src/server/care-agent.js`
- 新增 `test/codex-conversation.test.js`
- 新增 `test/care-agent.test.js`
- 修改 `src/server/pet-agent.js`
- 修改 `src/server/index.js`
- 修改 `test/pet-agent.test.js`

### 验证

```bash
node --test test/codex-conversation.test.js test/care-agent.test.js test/pet-agent.test.js
npm run check
npm run smoke:codex
```

### 验收条件

- 同一 Care 线程先看到图片，再能正确引用此前画面回答用户语音文本。
- 静默观察不会向设备输出无意义消息。
- JSON 非法、动作未知或范围越界时不会执行动作。
- 原有 Pet 闲聊和 Codex 命令确认测试继续通过。
- 陪伴线程不具备写文件或执行命令的权限。

### 中文 commit

`统一多模态陪伴会话`

---

## 阶段 3：实现 Bridge 主动拍照与动态观察调度

### 目标

让 Bridge 可以主动要求已认证 Tab5 拍照，并根据 AI 返回值动态安排下一次观察。

### 具体工作

1. 新增 `ObservationScheduler`：
   - 注入时钟和随机数，保证可确定测试。
   - 默认随机 10～30 分钟。
   - 接受 AI 返回的 1～120 分钟下一次观察值。
   - 90 秒技术防重复。
   - 对话、录音、朗读、传图期间暂停。
   - 断线时取消，USB/Wi-Fi 恢复后重新安排。
2. 为设备协议加入 Bridge → 设备命令 `camera.capture`。
3. 将协议版本升级为 v5，并把最低固件版本同步升级。
4. `DeviceHub` 选择同一设备最高优先级的 USB/Wi-Fi 会话发送命令。
5. 固件处理 `camera.capture`：
   - 麦克风、摄像头或资源传输忙时明确拒绝。
   - 空闲时复用现有 `captureAndQueue`。
   - 返回关联的命令结果和捕获编号。
6. `VisionAgent` 完成分析后将图片交给 `CareAgent`，不再创建独立、失去上下文的视觉线程。

### 预计文件

- 新增 `src/server/observation-scheduler.js`
- 新增 `test/observation-scheduler.test.js`
- 修改 `src/shared/device-protocol.js`
- 修改 `src/server/device-hub.js`
- 修改 `src/server/vision-agent.js`
- 修改 `src/server/index.js`
- 修改 `firmware/src/firmware_app.cpp`
- 修改固件协议和对应测试

### 验证

```bash
node --test test/observation-scheduler.test.js test/device-protocol.test.js test/device-hub.test.js test/vision-agent.test.js
npm run test:firmware
npm run check
```

### 验收条件

- Bridge 可主动触发虚拟 Tab5 拍照。
- BLE 会话不会接收拍照命令。
- 设备忙时不会重入摄像头或麦克风。
- 90 秒内的重复调度只产生一次捕获。
- AI 返回不同复查时间时，下一次调度正确更新。
- 重连不会重复执行旧拍照命令。

### 中文 commit

`实现主动拍照与动态观察调度`

---

## 阶段 4：实现无需反复按键的多轮语音对话

### 目标

AI 主动开口后，Tab5 在朗读结束时自动监听用户，并持续进行多轮对话。

### 具体工作

1. 为语音模式加入 `care`，保持 `chat` 和 `command` 行为不变。
2. `VoiceAgent` 在 `care` 模式完成转写后调用同一个 `CareAgent`，而不是调用普通 `PetAgent.chat`。
3. 扩展 AI 回复设备事件：
   - `care.reply`
   - `continueListening`
   - `nextObservationMinutes`
   - 可选动作状态
4. 为 `DeviceAudio` 增加可查询的播放完成状态或完成回调。
5. 固件收到 `continueListening=true` 时：
   - 先完整朗读回复。
   - 朗读结束后自动启动 `care` 录音。
   - UI 显示“正在听”并允许手动停止。
6. 为自动录音加入简单本地语音活动判断：
   - 最长录音时间默认 20 秒。
   - 检测到用户开口后，持续静音约 1.5 秒自动结束。
   - 一直没有开口则超时返回安静状态，不向 AI 伪造文本。
7. 用户说出结束语义后，由 AI 返回 `continueListening=false`，自然结束本次会话。

### 预计文件

- 修改 `src/server/voice-agent.js`
- 修改 `src/server/care-agent.js`
- 修改 `src/server/device-hub.js`
- 修改 `src/shared/device-protocol.js`
- 修改 `firmware/src/device_voice.hpp`
- 修改 `firmware/src/device_voice.cpp`
- 修改 `firmware/src/device_audio.hpp`
- 修改 `firmware/src/device_audio.cpp`
- 修改 `firmware/src/firmware_app.cpp`
- 修改 `firmware/src/tab5_ui.cpp`
- 修改相关 JavaScript 与原生固件测试

### 验证

```bash
node --test test/voice-agent.test.js test/care-agent.test.js test/device-hub.test.js
npm run test:firmware
npm run check
```

### 验收条件

- AI 开场后不按按钮也会进入聆听。
- 用户说完后自动提交一轮并得到同一上下文回复。
- 至少完成三轮连续模拟对话。
- AI 朗读时不会同时启动麦克风。
- 无人回应时在超时后安静退出。
- `command` 模式仍必须经过原有明确确认。

### 中文 commit

`实现主动关怀多轮语音对话`

---

## 阶段 5：实现 AI 白名单动作与执行反馈

### 目标

让 AI 可以在对话中执行有界动作，并把执行结果送回同一会话继续交流。

### 第一批动作

1. `capture_now`
2. `set_tab5_brightness`
3. `set_tab5_volume`
4. `open_app`
5. `open_media_preset`
6. `set_macos_volume`
7. `schedule_follow_up`

### 具体工作

1. 新增 `CareActionService`：
   - 严格动作注册表。
   - 每个动作独立参数验证。
   - 命令去重和超时。
   - 统一结果结构。
2. 新增设备动作命令：
   - 设置屏幕亮度。
   - 设置设备音量。
   - 可选持续时间结束后恢复原值。
3. 新增 macOS 动作适配器：
   - 只允许设置 0～100 的数字音量。
   - 只允许打开设置文件中的固定 bundle id。
   - 只允许打开预设媒体 URL。
   - 使用依赖注入的进程启动器，测试中不真实打开应用。
   - 不接受 AI 生成的任意 Shell、AppleScript 或 URL。
4. 动作执行完成后，把成功、失败和实际值写回 Care 线程，再让 AI 继续回复。
5. 动作失败不能中断整个对话。

### 预计文件

- 新增 `src/server/care-action-service.js`
- 新增 `src/server/macos-care-actions.js`
- 新增对应测试
- 修改 `src/server/care-agent.js`
- 修改 `src/server/device-hub.js`
- 修改 `src/shared/device-protocol.js`
- 修改 `firmware/src/firmware_app.cpp`
- 修改设备音频、显示和协议测试

### 验证

```bash
node --test test/care-action-service.test.js test/macos-care-actions.test.js test/care-agent.test.js test/device-hub.test.js
npm run test:firmware
npm run check
```

### 验收条件

- AI 可以打开配置好的网易云音乐应用预设。
- AI 不能打开未配置应用、任意 URL 或执行任意命令。
- 设备亮度和音量可以修改并按计划恢复。
- 动作结果进入同一 AI 上下文，下一句能准确说明执行结果。
- 重复动作消息不会执行两次。

### 中文 commit

`接入主动关怀白名单动作`

---

## 阶段 6：补齐电脑控制面板与 Tab5 关怀界面

### 目标

让个人使用者可以直接配置、观察和调试主动关怀行为，并在 Tab5 上清楚看到当前对话状态。

### 具体工作

1. 电脑控制面板新增“主动关怀”区域：
   - 总开关。
   - 普通观察最小/最大间隔。
   - 自动聆听时长。
   - AI 人设文本。
   - 应用和媒体预设。
   - 允许的自动动作。
   - “立即观察”测试按钮。
   - 当前关怀状态和下一次观察时间。
2. 新增有 CSRF 保护的 API：
   - 获取/更新关怀设置。
   - 立即观察。
   - 停止当前关怀会话。
   - 读取有界事件摘要。
3. Tab5 增加关怀状态呈现：
   - 正在观察。
   - 正在思考。
   - 正在说话。
   - 正在听。
   - 正在执行动作。
4. 复用现有 Pet 动画，不扩展图集：
   - 观察/思考使用 `review`。
   - 说话使用 `waving`。
   - 聆听使用 `waiting`。
   - 动作使用 `running`。
5. 提供显式“停止对话”入口，停止后不改变长期总开关。

### 预计文件

- 修改 `public/index.html`
- 修改 `public/app.js`
- 修改 `public/app.css`
- 修改 `src/server/http-server.js`
- 修改 `src/server/settings-repository.js`
- 修改 `src/server/desk-store.js`
- 修改 `firmware/lib/codex_core` 类型和解析
- 修改 `firmware/src/tab5_ui.hpp`
- 修改 `firmware/src/tab5_ui.cpp`
- 修改 HTTP、Store 和固件 UI 测试

### 验证

```bash
node --test test/http-server.test.js test/desk-store.test.js
npm run test:firmware
npm run check
```

并在 Mock 模式完成浏览器直接验收：

- 设置可保存并重启恢复。
- “立即观察”只触发一次。
- 所有关怀状态能正确呈现。
- 控制台没有错误。

### 验收条件

- 用户不编辑文件即可配置全部主动关怀参数。
- API 不接受跨站修改。
- Tab5 与浏览器模拟器状态一致。
- 停止当前对话后，未来观察仍可继续。

### 中文 commit

`完善主动关怀配置与设备界面`

---

## 阶段 7：完成虚拟设备闭环与故障恢复

### 目标

在没有真机的情况下，证明主动观察、多轮对话、动作和重连构成一个完整、可重复的闭环。

### 具体工作

1. 扩展虚拟 Tab5：
   - 接收主动拍照命令。
   - 发送固定 JPEG。
   - 模拟 TTS 完成。
   - 模拟自动录音和三轮语音转写。
   - 接收亮度、音量和恢复命令。
2. 新增完整验收场景：
   - Bridge 定时触发拍照。
   - AI 观察后主动开口。
   - 设备自动聆听。
   - 用户连续回答三轮。
   - AI 打开一个测试预设并调节设备。
   - 动作结果返回 AI。
   - AI 安排下一次观察。
3. 覆盖故障：
   - 图片传输中断。
   - AI JSON 非法。
   - Codex 断开与重连。
   - USB/Wi-Fi 主链切换。
   - 重复拍照或动作命令。
   - 对话期间设备掉线。
4. 对所有计时器使用可控时钟，避免依赖真实等待。

### 预计文件

- 修改 `scripts/verify-virtual-tab5.mjs`
- 修改 `src/server/stability-suite.js`
- 新增或修改关怀端到端测试
- 修改协议、设备会话和故障注入测试

### 验证

```bash
npm run check
npm run test:virtual-tab5
npm run test:stability
npm run smoke:codex
```

### 验收条件

- 虚拟设备完成至少三轮主动关怀对话。
- 每个动作只执行一次。
- USB/Wi-Fi 切换后对话状态和下一次观察不丢失。
- AI 或图片失败后调度器可以恢复，不形成忙循环。
- 全部既有测试和新增测试通过。

### 中文 commit

`完成主动关怀虚拟设备闭环`

---

## 阶段 8：版本发布、文档与软件完成审计

### 目标

把软件升级为可发布版本，并按最终目标逐条证明已经完成。

### 具体工作

1. 将项目、Bridge、协议和固件版本统一升级到 `0.3.0`。
2. 更新：
   - `README.md`
   - `docs/device-protocol.md`
   - `docs/firmware-audio.md`
   - `docs/acceptance.md`
   - `docs/install-and-recovery.md`
3. 文档必须说明：
   - 不需要 OpenAI API Key。
   - 需要 Codex App Server 保持登录和运行。
   - 主动摄像头观察策略。
   - 半双工多轮对话。
   - 动作白名单和预设配置。
   - 故障恢复与停止方式。
4. 生成完整固件发布包并复验哈希。
5. 对最终目标的十项要求逐项链接到代码、测试或发布产物证据。

### 验证

```bash
npm run check
npm run test:virtual-tab5
npm run test:stability
npm run doctor
npm run smoke:codex
npm run release:firmware
git diff --check
```

### 验收条件

- 所有软件自动化验证通过。
- 完整工厂镜像与清单生成成功并通过哈希复验。
- 文档没有保留“摄像头只能手动触发”或“视觉禁止状态推断”等过期描述。
- 验收矩阵明确区分软件完成和仍待真机的物理证据。
- 工作区只包含计划内变更。

### 中文 commit

`发布零点三版主动关怀系统`

---

## 阶段 9：真机烧录与物理完成验收

### 目标

在 Tab5 K145 真机上证明摄像头、麦克风、扬声器、屏幕和无线链路可以长期完成主动关怀闭环。

### 具体工作

1. 使用完整 `0.3.0` 工厂镜像烧录真机。
2. 验证普通状态下 10～30 分钟随机拍照。
3. 验证摄像头方向、曝光和传输时间。
4. 完成至少五轮连续自动聆听对话：
   - AI 主动开口。
   - TTS 播放完成。
   - 自动打开麦克风。
   - 静音检测自动结束。
   - AI 继续回答。
5. 验证：
   - Tab5 亮度调节和恢复。
   - Tab5 音量调节和恢复。
   - macOS 音量。
   - 网易云音乐或用户配置应用。
   - 立即再拍和动态复查。
6. 连续运行至少 24 小时，记录：
   - 主动观察次数。
   - 失败与恢复次数。
   - 误重复次数。
   - 内存趋势。
   - Wi-Fi 重连。
   - 电量与温升。
7. 将真实结果写入 `docs/acceptance.md`，不把未观察到的事实写成通过。

### 验证

```bash
npm run release:firmware
npm run flash:firmware -- --port /dev/cu.usbmodemXXXX
npm run doctor
npm run acceptance:hardware -- --device-id <已配对设备 ID>
```

`acceptance:hardware` 每 30 秒原子保存一次检查点，并在 24 小时结束时自动审计五轮会话、动作、重复、失败恢复、Wi-Fi 重连、Bridge RSS、电量和芯片温度；进程中断后使用 `--resume <报告路径>` 继续。保存最终报告、Bridge 诊断摘要和验收结论。

### 验收条件

- 真机完成主动拍照、AI 开场、五轮对话、动作执行和动态复查。
- 24 小时内没有重复动作、忙循环、无法恢复的断线或持续内存增长。
- 语音、摄像头、设备动作和温升均有真实证据。
- `docs/acceptance.md` 中所有主动关怀项目均有明确结论。

### 中文 commit

`完成主动关怀真机验收`

---

## 四、全项目完成判定

只有同时满足以下条件，整个目标才算完成：

1. 阶段 0～9 均有对应中文 commit。
2. 工作区干净，没有临时脚本、调试输出、未使用代码或 TODO 噪声。
3. `npm run check`、虚拟设备、稳定性、doctor、Codex 冒烟和固件发布全部通过。
4. 真机至少完成一次五轮主动对话和 24 小时运行。
5. 摄像头观察、对话和动作全部使用同一个 Care 上下文或可验证的记忆续接。
6. 没有 OpenAI API Key 依赖。
7. 没有固定 6 小时关怀冷却。
8. AI 可以保持安静、主动开口、倾听、继续对话、执行动作和跟进，而不是单方面输出。
