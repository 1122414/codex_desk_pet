# Codex Desk Buddy 完整 MVP 路线与验收矩阵

## 已确认目标

- 目标开发硬件固定为 M5Stack CoreS3 完整版，SKU `K128`，不再以 CoreS3 Lite 为目标。
- 设备允许通过 USB-C 使用，也必须能拔线后通过无线与仍在运行的电脑 Bridge 通信。
- Wi-Fi 是日常主链路；Bluetooth LE 用于首次配网、近距离控制与恢复；USB CDC 用于烧录、调试和可靠备用。
- 不要求电脑关机后继续获得 Codex 实时任务，也不设 6 小时或 8 小时的脱线续航目标。
- Bridge 是设备、电脑控制面板和 Pet 选择的同步源；在 Codex 提供公开 Pet 事件前，不写入官方客户端私有设置。
- 新 Pet 继续只在电脑端制作；设备负责接收、校验、缓存、切换和播放。

## 需求验收矩阵

| 编号 | 需求 | 无设备阶段的完成证据 | 真机到手后的补充证据 |
| --- | --- | --- | --- |
| R1 | 320×240 触摸界面 | 像素尺寸固定的浏览器模拟器、触摸和滑动自动化测试 | 屏幕方向、触摸坐标和手感实测 |
| R2 | PC 端全部 Pet 状态和动画 | v1/v2 图集合同、9 个标准动画、16 个 v2 看向方向及逐项预览 | WebP 解码内存、帧率和无撕裂实测 |
| R3 | 设备允许/拒绝审批 | 当前 App Server schema 契约测试、请求关联、过期与重复决策测试 | 实体触摸/按键防误触实测 |
| R4 | 内置与自定义 Pet | 安全目录扫描、manifest 与图集尺寸/hash 校验、回退 Pet | SD 卡缓存和损坏回退实测 |
| R5 | 设备与电脑切换 Pet | Bridge 单一选择状态、控制面板和设备协议命令测试 | 触屏/外接按键与双链路切换实测 |
| R6 | 最近运行任务 | App Server 事件归一化、优先级和最近活动线程测试 | 长时间多线程实际运行观察 |
| R7 | 声音、语音、时钟、电量、Token、等级 | 模拟器、快照字段和设备命令合同测试 | 扬声器、麦克风、RTC、电池读数实测 |
| R8 | USB、Wi-Fi、BLE | 统一协议、虚拟传输、认证配对、ACK、重连和离线恢复测试 | USB CDC、Wi-Fi 和 BLE 真机互操作 |
| R9 | 产品稳定性 | 故障注入、重复命令、断线重连、资源原子更新和端到端压力测试 | 72 小时、路由器重启、断电和 500 次链路切换 |

## 当前基线证据

- `npm run check`：27/27 项测试通过。
- `npm run smoke:codex`：使用本机 `codex-cli 0.145.0-alpha.18` 成功初始化 App Server 并读取线程。
- Playwright 实机浏览器验收：设备屏实际渲染为 320×240；9/9 动画与 16/16 v2 方向逐项可操作；真实 v1 Pet 正确禁用方向；电脑下拉、设备箭头和键盘切换保持同步；审批允许后弹层清除。
- 本机生成的 App Server schema 已确认：
  - `item/commandExecution/requestApproval`
  - `item/fileChange/requestApproval`
  - `item/tool/requestUserInput`
  - `serverRequest/resolved`
  - 单次审批决定 `accept` / `decline`
- 当前缺口不是界面原型，而是真实传输服务、Pet 资源同步、自动恢复、可编译 CoreS3 固件和覆盖这些链路的端到端测试。

## 实施阶段

1. 校准 Codex Bridge（已完成）
   - 对齐当前 App Server schema。
   - 完成错误、重试、用户输入、审批失效和自动重连。
2. 完成 Pet 与模拟器（已完成）
   - 校验真实图集尺寸和格式。
   - 完成 Pet manifest/hash、状态动画、方向和界面自动化测试。
3. 完成设备服务
   - 实现统一会话、配对、序号、ACK、重发、快照恢复和资源分块传输。
   - 用内存虚拟 USB/Wi-Fi/BLE 传输执行故障测试。
4. 完成 CoreS3 K128 固件
   - 使用 PlatformIO/Arduino 与 M5Unified。
   - 将业务状态机和协议解析保持为可在主机测试的纯 C++ 模块。
   - 真机驱动隔离在显示、触摸、音频、电源、SD、USB、Wi-Fi、BLE 适配层。
5. 完成交付审计
   - 自动测试、真实 Codex 冒烟、浏览器视觉检查、固件主机测试和构建检查。
   - 提供从零开始的安装、烧录、配网、恢复和验收说明。

## 真机前允许保留的未验证项

只有依赖物理特性的项目可以留到设备到手后：屏幕色彩和刷新、触摸精度、实际声音、电池曲线、天线环境、microSD 实际吞吐及真机固件烧录。所有协议、状态、资源、错误恢复和固件可测试核心必须提前完成。
