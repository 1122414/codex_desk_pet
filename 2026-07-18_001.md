# Codex Desk Buddy MVP 实施计划

## 目标

先在电脑上交付一个可运行、可测试的 320×240 触摸设备模拟器，并通过 Codex App Server 接入真实线程状态。硬件到手后，CoreS3 Lite 固件复用同一套状态模型和设备协议，不重写业务逻辑。

## 已确认需求

- 目标硬件：M5Stack CoreS3 Lite，320×240 电容触摸屏。
- 展示 Codex Pet 支持的全部动画：9 个标准状态和 16 个看向方向。
- 支持命令执行和文件修改的允许/拒绝；设备只提供单次允许和拒绝，避免误触产生会话级永久授权。
- 内置 Pet 和电脑端新建的 Pet 都可使用；新 Pet 只在电脑端制作。
- Pet 可从触屏、电脑控制面板切换，并通过 Bridge 同步到所有已连接设备。
- 默认显示最近活动的 Codex 线程；等待审批的线程具有更高显示优先级。
- MVP 包含声音、语音播报、时钟、电池、Token 和等级展示。
- USB 常联和 Wi-Fi 都使用统一消息协议；Wi-Fi 必须支持心跳、断线重连、序号去重和全量状态恢复。
- 最终目标是可产品化，因此审批、路径、重连和错误状态不能只按演示实现。

## 技术事实与边界

- Codex App Server 提供线程状态、Turn/Item 生命周期、Token 用量和审批请求，可作为真实数据源。
- Codex Pet v2 图集固定为 1536×2288，单格 192×208，8 列×11 行；v1 图集为 1536×1872。
- 当前 App Server 协议没有 Pet 列表或 Pet 选择事件。MVP 由 Codex Desk Bridge 保存当前 Pet，并同步电脑控制面板和设备。原生 Codex 客户端选择状态预留适配器；在出现稳定公开接口前不写入私有客户端状态。
- 电脑 MVP 的电池值来自设备遥测模拟；连接真机后改为真实电量。
- 等级不是 Codex 原生概念。MVP 按当前展示线程每累计 50,000 Token 升一级，阈值集中配置，后续可调整。

## MVP 架构

```text
Codex App Server (stdio JSON-RPC)
              │
              ▼
Codex Desk Bridge ───── Pet 目录 (~/.codex/pets)
      │       │
      │       └──── HTTP + SSE ──── 电脑控制面板 / 320×240 模拟器
      │
      └──── 设备协议 ──── USB Serial 或 Wi-Fi 长连接（硬件阶段）
```

Bridge 是唯一状态源：归一化线程状态、选择最近活动任务、保存当前 Pet、验证审批请求，并向所有客户端广播全量快照或增量事件。

## 状态映射

| Codex 事件 | 设备状态 | Pet 动画 |
| --- | --- | --- |
| 无活动线程 | ready | idle |
| thread active | running | running |
| waitingOnApproval | needs-input | waiting |
| waitingOnUserInput | needs-input | waiting |
| review item / review turn | reviewing | review |
| turn completed | completed | waving |
| turn failed / systemError | blocked | failed |
| 触摸拖动 Pet | local-interaction | running-left / running-right |
| 触摸看向位置 | local-interaction | 16 个 look direction |

`jumping` 用于完成态短暂庆祝；`waving` 随后播放。所有标准行都可在开发面板中直接预览和验证。

## 阶段与验收

1. 规格与技术核对
   - 固定 MVP 范围、状态映射、Pet 格式和协议约束。
2. 共享模型与协议
   - 状态归一化、最近线程选择、Token/等级、消息序号/确认/心跳。
   - 使用 Node 内置测试覆盖关键逻辑。
3. 真实 Codex Bridge
   - App Server 初始化、线程列表、事件流、审批响应、Pet 安全目录读取。
   - 无 Codex 或测试环境下提供明确的 mock 模式。
4. 触摸屏模拟器
   - 精确 320×240 画布、Pet 图集播放、状态栏、任务摘要、审批和切换交互。
   - 电脑侧提供 Pet/状态/连接控制和开发预览。
5. 集成与交付
   - 单元测试、API 冒烟测试、浏览器视觉检查、运行文档和硬件接入清单。

## MVP 不包含

- CoreS3 Lite 真机固件和烧录（购买硬件后的下一阶段）。
- 在设备上生成或编辑新 Pet。
- 通过未公开的 Codex 客户端私有配置强行同步 Pet。
- 面向公网的远程访问；MVP 默认只监听本机回环地址。

## 验证命令

```bash
npm test
npm run check
npm start
```
