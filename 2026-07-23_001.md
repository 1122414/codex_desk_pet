# Tab5 K145 迁移计划

## 已确认的产品边界

- 目标硬件固定为 M5Stack Tab5 Kit `K145`，包含 NP-F550 电池；不再支持 CoreS3 K128。
- MVP 首次账户配对走 USB，日常状态、Pet 同步与控制走 2.4 GHz Wi-Fi。ESP32-P4 通过 ESP32-C6 使用无线网络，实机验证完成前不承诺 BLE 配网。
- Tab5 的 2MP 摄像头保留为后续扩展；本阶段不采集、不上传、不识别任何画面。
- Pet 源文件仍在电脑端制作。设备使用 384×416、透明 RGB565 帧，完整 v2 Pet 最大为 28,114,944 Bytes，缓存到 microSD。

## 分阶段交付

1. 固化 Tab5 的板型、设备能力和大屏 Pet 资源合同；旧 CoreS3 设备将明确显示为不兼容。
2. 使用 Tab5 官方推荐的 ESP32-P4/PioArduino 构建链，适配 USB、Wi-Fi、microSD、电源、RTC、扬声器和触摸。
3. 将 320×240 界面替换为 1280×720 触摸界面，保持状态、运行记录、审批、Pet 切换与配对能力；审批仍只有全部详情可见时允许确认。
4. 更新发布、刷机、恢复和验收资料；用无硬件测试验证协议、资源和安全流程。屏幕、触摸、无线、电池、音频、TF 卡与摄像头仅在实机到手后验收。
