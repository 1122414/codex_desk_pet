# CoreS3 K128 硬件与固件计划

## 购买清单

- M5Stack CoreS3 完整版，SKU `K128`，包含 500mAh 电池 DinBase。
- 支持数据传输的 USB-C 线；仅充电线不能用于烧录和 USB 常联。
- 8–32 GB、可格式化为 FAT32 的可靠 microSD 卡，用于缓存多个 Pet 图集。
- 稳定的 2.4 GHz Wi‑Fi 网络；ESP32-S3 不使用 5 GHz Wi‑Fi。
- Mini Dual Button Unit `U025`，用于独立的允许/拒绝与组合切换操作。
- MVP 不要求电脑关机后继续接收 Codex 任务，也不设 6 小时或 8 小时续航目标。

## 固件最小模块

1. 显示层
   - 320×240 UI，与电脑模拟器保持同一布局信息层级。
   - v1/v2 WebP 图集解码、逐帧时序和内置回退 Pet。
2. 输入层
   - 触摸看向、滑动换 Pet、实体按键换 Pet。
   - 审批防误触：按下反馈、释放确认和短暂冷却时间。
3. 传输层
   - USB CDC 常联。
   - 带认证、AES‑256‑GCM、心跳、重连、ACK、快照恢复的 Wi-Fi WebSocket。
   - Bluetooth LE 配网、近距离控制和恢复通道。
4. 资源层
   - microSD Pet 缓存、hash 校验、分块更新和原子切换。
5. 设备层
   - 电池、充电状态、Wi‑Fi RSSI、时钟同步、扬声器提示和音量设置。

## 开发顺序

1. 在没有真机时完成可在主机测试的 C++ 状态机、协议、资源 manifest 和故障测试。
2. 完成 PlatformIO CoreS3 K128 工程以及显示、触摸、按键、音频、电源、SD、USB、Wi-Fi、BLE 适配层。
3. 真机到手后用 USB 烧录最小诊断程序，验证屏幕、触摸、按键、扬声器和电池读取。
4. 播放 microSD 中的固定 v2 Pet，并验证解码内存与帧率。
5. 依次验证 USB、Wi-Fi、BLE、审批和资源更新。
6. 完成掉电恢复、长时间运行和故障回退。

## 产品质量验收

- 连续运行 72 小时无崩溃、明显内存增长或 UI 卡死。
- Wi‑Fi 断开、路由器重启和电脑睡眠后自动恢复，不重复执行命令。
- USB/Wi‑Fi 同时切换 500 次不重复审批、不丢失当前 Pet。
- 随机断电不会破坏已安装 Pet；损坏资源自动回退到内置 Pet。
- 触摸审批不存在一次点击多次提交，过期审批永远不能执行。
- 9 个标准动画和 v2 的 16 个方向逐项视觉检查通过。
- 满电、低电、充电、弱 Wi‑Fi、无 SD 卡、Codex 离线都有明确状态。

## 当前阻塞项

- 需要拿到 CoreS3 K128 才能确认 WebP 解码内存、microSD 吞吐、触摸手感、扬声器音量和无线射频表现。
- Codex 原生客户端尚无公开 Pet 选择事件；真机先以 Desk Bridge 为同步源。
- 协议 v2 已完成 HMAC 双向认证和 AES‑256‑GCM 应用层加密，并通过真实 ESP32-S3 工具链构建；真机阶段只需测量吞吐、延迟和长稳表现。
