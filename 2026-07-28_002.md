# Chibi Skadi Q 版主题与会话详情

## 目标

为 Tab5 上的 Chibi Skadi 系列 Pet 提供独立的 Q 版冰海主题，并让任务列表明确区分「项目」与「纯对话」。点击任一卡片后，设备进入只读会话详情页；电脑控制页提供同一分类与详情能力。

## 范围

### 主题

- 仅当 `selectedPetId` 为 `chibi-skadi` 或以 `chibi-skadi-` 开头时启用。
- 背景使用深海蓝、冰晶青、珊瑚粉与珍珠白，不影响其他 Pet 的默认主题。
- 状态栏、电量、链路、额度、Token、任务卡、按钮与滚动条均使用 Skadi 专属图形语言。
- 中文继续使用固件现有 `efontCN_16`，通过字号、颜色、间距、标签与装饰建立字体层级，不增加字体依赖。
- 动画区保持独立局部刷新，避免主题绘制降低宠物帧率。

### 项目与对话

- 初始分类规则保持可解释和稳定：
  - 线程含 `gitInfo`：`project`
  - 线程不含 `gitInfo`：`conversation`
- 项目卡使用仓库/冰晶徽记、直角切角与青色侧边带。
- 对话卡使用对话气泡、圆角轮廓与珊瑚粉点缀。
- 卡片均显示标题、状态、更新时间与 Token；项目额外显示工作区或仓库名（若 Codex 提供）。

### 会话详情

- 点击卡片与拖动滚动分离：移动不超过 14 像素才视为点击。
- 设备发送 `thread.open`，Bridge 校验线程必须存在于当前列表后调用 Codex `thread/read`。
- 只下发最近的用户消息和助手消息，不下发 reasoning、工具参数、命令日志、图片或内部事件。
- 设备响应最多 12 条消息、每条最多 420 UTF-8 字节、整体保持在 8 KiB 传输上限内。
- 详情页保留左侧 Pet，右侧展示标题、类型徽记、消息气泡、加载/失败/空状态与返回按钮。
- 详情页支持触摸滚动，返回后保留任务列表滚动位置。
- 电脑控制页使用本地会话 Cookie 访问同一只读接口，并以对话框展示详情。

## 安全与兼容

- HTTP 会话详情接口要求本地会话 Cookie、同源请求与已存在的线程 ID。
- 所有浏览器内容只通过 `textContent` 写入。
- 设备命令为现有 v4 协议的向后兼容扩展，不更换配对密钥。
- Bridge 与固件功能版本升为 `0.3.0`；旧固件继续接收快照，但没有详情交互。

## 可验证结果

1. Chibi Skadi 与其他 Pet 切换时主题正确启用/恢复。
2. 项目卡与对话卡在真机和电脑端视觉上明显不同。
3. 轻点任务打开正确线程；拖动列表不会误开详情。
4. 详情不包含 reasoning 或工具内部内容，超长文本被安全截断。
5. 消息详情在 USB、Wi-Fi、BLE 的 8 KiB 上限内可传输。
6. 真机详情可滚动、可返回，宠物动画仍持续播放。
7. Node 测试、固件静态检查、固件原生测试与 Tab5 release build 全部通过。
