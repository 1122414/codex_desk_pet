# Codex Desk Buddy 真机前完成计划

## 目标边界

设备固定为 M5Stack CoreS3 完整版 `K128`。真机到手前，项目必须达到：

- PC Bridge、管理页面和 CoreS3 固件均可构建、可自动测试。
- 设备首次用 USB 安全配对，此后可只用 Wi‑Fi 日常运行；USB 仍是可靠备用链路，BLE 负责安全写入网络配置。
- 任务、审批、Pet、动画、Token、等级、时钟、电量和声音在统一快照中同步。
- 所有不依赖屏幕、触摸、扬声器、电池、天线和 microSD 物理特性的逻辑都有直接证据。
- 真机阶段只保留烧录、驱动校准、射频/功耗/声音实测及 72 小时物理长稳测试。

明确不做：电脑关机后继续获取 Codex 实时状态；承诺 6 小时或 8 小时续航。

## 2026-07-20 权威基线

- 本机 `codex-cli 0.145.0-alpha.18` 可生成 App Server JSON Schema。
- 当前公开 App Server 仍提供 `thread/list`、线程/Turn/Item 生命周期、Token 事件、命令审批、文件修改审批和用户输入请求。
- 当前 Schema 还包含 `item/permissions/requestApproval`；Bridge 已与命令、文件审批一起呈现给设备，并在详情不完整时关闭允许按钮。
- 当前 App Server Schema 中没有 Pet 列表或 Pet 选择事件。因此 Bridge 继续作为设备和本项目控制面的 Pet 同步源；不得声称能与 Codex 官方客户端的选择双向同步。
- 官方文档把公开 Pet 状态定义为 Running、Needs input、Ready、Blocked，并说明多任务优先级为 Needs input、Blocked、Ready、Running。项目额外保留图集中的 9 个动画和 16 个看向方向，但要把“公开状态”和“可播放动画”分开表述。
- 官方将 App Server 定位为深度产品集成接口，但同时说明其本地开发接口可能变化；发布包必须带版本兼容检查，不能仅依赖记忆中的 Schema。

## 完成度审计

| 领域 | 当前直接证据 | 剩余边界 | 结论 |
| --- | --- | --- | --- |
| Codex 数据源 | 真实 App Server 冒烟、当前 Schema 诊断、最近任务和三类审批合同测试 | App Server 官方标记为实验性，Codex 更新后必须重跑诊断 | 真机前完成 |
| Pet | v1/v2 校验、9 个动画、16 个方向、WebP 转 RGB565、模拟器视觉验收、资源恢复与双槽回退 | 官方没有公开 Pet 选择事件，Bridge 只能同步本项目界面与设备 | 公开限制已明确 |
| USB/Wi‑Fi/BLE | 协议 v3、HMAC、AES‑256‑GCM、篡改/降级测试、重连、ACK、500 次切换；BLE Secure Connections + MITM 配网 | 真实驱动、射频和系统蓝牙体验 | 代码完成，待真机 |
| 审批安全 | 三类审批、原始 RPC ID、完整摘要、单次决策、过期和跨链路去重 | 真机小屏实际可读性 | 真机前完成 |
| 设备资源 | 分块/整包哈希、断点续传、冲突拒绝、不可变路径、双槽 generation、250 次中断恢复和 50,000 次指针循环 | microSD 实际兼容性和物理断电 | 代码完成，待真机 |
| 音频 | 浏览器语音、固件 ESP-SR 离线中文 TTS、独立可打断任务和非阻塞降级音型 | 扬声器音质、音量与温升 | 代码完成，待真机 |
| PC 管理 | HTTP/SSE、CSRF、配网、Pet、模拟器、设备兼容诊断、恢复向导和主机 `doctor` | 无 | 真机前完成 |
| 稳定性 | 全量 JS/协议/C++、真实工具链、500 次切换、四类协议故障、250 次资源恢复、50,000 次断电指针与序号循环 | 射频、电源、存储、温度和 72 小时物理长稳 | 代码完成，待真机 |
| 发布 | 固定依赖、双 OTA 分区、完整工厂镜像、逐组件 SHA‑256、显式串口烧录和完整恢复说明 | Secure Boot/Flash Encryption/签名升级需量产密钥与不可逆 eFuse 真机流程 | 工厂恢复完成，量产安全待真机 |

## 实施顺序

1. 协议与审批安全：AES-256-GCM、拒绝明文降级、权限审批、完整审批摘要。
2. 资源与音频可靠性：断电恢复、冲突块拒绝、非阻塞音频和设备端中文短句。
3. 管理与发布：设备诊断、版本能力、配网/恢复、固件包和校验清单。
4. 自动验证：端到端、随机故障、断电模型、500 次切换、长循环和真实 Codex 冒烟。
5. 逐项交付审计：每项需求必须链接到源代码、测试命令或真机验收步骤。

## 真机到手后唯一允许保留的项目

- 屏幕色彩、撕裂、帧率和触摸坐标/手感。
- 外接按键、扬声器音量、RTC、电池曲线和充电状态。
- 2.4 GHz Wi‑Fi、BLE 射频表现与电脑睡眠/路由器重启实测。
- microSD 实际吞吐、随机断电物理测试和连续 72 小时运行。
- 首次烧录、签名升级/回退的物理设备闭环。

## 阶段记录

- 协议与审批安全已关闭：协议升级为 v3，认证后全部 payload 使用方向隔离的 AES‑256‑GCM；设备版本和能力哈希进入双向认证；拒绝篡改和明文降级；BLE 配网强制 Secure Connections + MITM 并轮换配网码；命令、文件和额外权限审批均只允许一次。
- 资源安全已关闭：重复资源区间必须与已写字节一致；active 指针改为双槽 generation 日志，新槽损坏或断电时保留上一份已验证资源。
- 设备音频已关闭真机前代码缺口：固定版本、固定哈希的 Espressif ESP-SR v1.2.0 提供离线中文 TTS；所有状态与关键设备事件使用独立可打断音频任务，语音分区损坏时降级为非阻塞音型。实际音质仍列入真机验收。
- 管理与发布已关闭代码缺口：认证设备上报板型、固件、协议、语音和存储健康状态，控制面板显示兼容结论；发布脚本生成并复验完整工厂镜像，烧录脚本要求显式串口且只在 `--erase` 时整片擦除；当前 BLE 仅用于安全配网，不虚构尚不存在的电脑端原生 BLE 账户配对。
- 自动稳定性已关闭代码缺口：固定种子的真实会话循环完成 500 次 USB/Wi‑Fi 切换，并注入重复包、丢失快照、乱序与 ACK 丢失；250 次 Pet 中断安装都保持旧版本、拒绝坏块并从缺口恢复；原生 C++ 再覆盖 50,000 次双槽断电、10,000 次乱序资源和 50,000 次序号循环。
- 安装与逐项验收已关闭代码缺口：`doctor` 从本机实际检查 Node、Codex 版本、实验性 App Server Schema、最近线程读取、PlatformIO 和完整固件包；最终矩阵把每条原始需求映射到实现、测试或唯一剩余的真机步骤。
