# Codex Desk Buddy 真机前完成计划

## 目标边界

设备固定为 M5Stack CoreS3 完整版 `K128`。真机到手前，项目必须达到：

- PC Bridge、管理页面和 CoreS3 固件均可构建、可自动测试。
- 设备首次用 USB 或 BLE 安全配对，此后可只用 Wi‑Fi 日常运行；USB 仍是可靠备用链路。
- 任务、审批、Pet、动画、Token、等级、时钟、电量和声音在统一快照中同步。
- 所有不依赖屏幕、触摸、扬声器、电池、天线和 microSD 物理特性的逻辑都有直接证据。
- 真机阶段只保留烧录、驱动校准、射频/功耗/声音实测及 72 小时物理长稳测试。

明确不做：电脑关机后继续获取 Codex 实时状态；承诺 6 小时或 8 小时续航。

## 2026-07-20 权威基线

- 本机 `codex-cli 0.145.0-alpha.18` 可生成 App Server JSON Schema。
- 当前公开 App Server 仍提供 `thread/list`、线程/Turn/Item 生命周期、Token 事件、命令审批、文件修改审批和用户输入请求。
- 当前 Schema 还包含 `item/permissions/requestApproval`；现有 Bridge 尚未把它呈现给设备。
- 当前 App Server Schema 中没有 Pet 列表或 Pet 选择事件。因此 Bridge 继续作为设备和本项目控制面的 Pet 同步源；不得声称能与 Codex 官方客户端的选择双向同步。
- 官方文档把公开 Pet 状态定义为 Running、Needs input、Ready、Blocked，并说明多任务优先级为 Needs input、Blocked、Ready、Running。项目额外保留图集中的 9 个动画和 16 个看向方向，但要把“公开状态”和“可播放动画”分开表述。
- 官方将 App Server 定位为深度产品集成接口，但同时说明其本地开发接口可能变化；发布包必须带版本兼容检查，不能仅依赖记忆中的 Schema。

## 完成度审计

| 领域 | 已有直接证据 | 当前缺口 | 真机前关闭条件 |
| --- | --- | --- | --- |
| Codex 数据源 | 真实 App Server 冒烟、当前 Schema 契约测试、最近任务和状态模型 | 权限审批未映射；缺少安装时版本/Schema 诊断 | 三类审批合同测试；诊断命令输出版本和能力 |
| Pet | v1/v2 校验、9 个动画、16 个方向、WebP 转 RGB565、模拟器视觉验收 | 官方 Pet 选择没有公开事件；设备端只能由 Bridge 同步 | 保持明确边界；补资源恢复和发布回滚测试 |
| USB/Wi‑Fi/BLE | 统一协议、HMAC 双向认证、重连、ACK、快照恢复 | 已认证后的任务与审批内容仍为明文；BLE 大资源不现实 | 会话级 AEAD、篡改/重放/明文降级测试；Wi‑Fi 为日常无线链路，BLE 明确用于配网与恢复 |
| 审批安全 | 原始 RPC ID 关联、单次允许/拒绝、过期和跨链路去重 | 设备只展示首个文件路径；多文件审批信息不完整；权限审批缺失 | 设备只能在完整可见摘要下允许；否则只可拒绝或回电脑 |
| 设备资源 | microSD 分块、块 hash、整包 hash、不可变资源路径 | active 指针更新存在断电窗口；重复范围可被覆盖 | 双槽指针或可恢复索引；已有字节冲突拒绝；主机故障模型测试 |
| 音频 | 浏览器中文语音、固件状态音型 | 固件没有真正的离线中文语音；部分提示使用阻塞延时 | 固定中文短句的可再分发资源或官方 ESP32-S3 TTS；音频调度不阻塞网络/UI |
| PC 管理 | 本机 HTTP/SSE、CSRF、配网、Pet 切换和模拟器 | 缺少固件产物/兼容性诊断和恢复向导 | 增加设备诊断、版本、配对/恢复状态及离线安装说明 |
| 稳定性 | 67 项 JS/协议测试、纯 C++ 测试、真实 ESP32-S3 构建 | 缺少长循环、随机掉包/乱序、断电存储和 500 次链路切换证据 | 自动化故障注入与长循环报告；真机项目单列 |
| 发布 | 固定 PlatformIO 依赖和 6 MiB 双 OTA 分区 | 没有可验证固件包、版本清单、升级/回退流程 | 可重复构建产物、SHA-256、兼容版本和安全恢复说明 |

## 实施顺序

1. 协议与审批安全：AES-256-GCM、拒绝明文降级、权限审批、完整审批摘要。
2. 资源与音频可靠性：断电恢复、冲突块拒绝、非阻塞音频和设备端中文短句。
3. 管理与发布：设备诊断、版本能力、配网/恢复、固件包和校验清单。
4. 自动验证：端到端、随机故障、断电模型、500 次切换、长循环和真实 Codex 冒烟。
5. 逐项交付审计：每项需求必须链接到源代码、测试命令或真机验收步骤。

## 真机到手后唯一允许保留的项目

- 屏幕色彩、撕裂、帧率和触摸坐标/手感。
- 外接按键、扬声器音量、RTC、电池曲线和充电状态。
- 2.4 GHz Wi‑Fi、BLE 射频表现与电脑睡眠/路由器重启实测。
- microSD 实际吞吐、随机断电物理测试和连续 72 小时运行。
- 首次烧录、签名升级/回退的物理设备闭环。

## 阶段记录

- 协议与审批安全已关闭：协议升级为 v2，认证后全部 payload 使用方向隔离的 AES‑256‑GCM；拒绝篡改和明文降级；BLE 强制 Secure Connections + MITM 并轮换配网码；命令、文件和额外权限审批均只允许一次。
- 资源安全已关闭：重复资源区间必须与已写字节一致；active 指针改为双槽 generation 日志，新槽损坏或断电时保留上一份已验证资源。
