# Tab5 MVP 最终验收与收尾计划

## 目标与边界

目标硬件固定为 M5Stack Tab5 Kit `K145`。本阶段完成 Codex Desk Buddy MVP 的功能闭环，并把量产级验证与公开接口限制明确分开：

- MVP 必须在电脑运行 Bridge 时展示真实 Codex 状态，支持 USB 和无线轻量同步、Pet 动画/切换、审批、语音对话/命令及拍照理解。
- ChatGPT 订阅登录不要求 OpenAI API Key。开发板录音由电脑本地 `whisper.cpp` 转写，文本和图片交给已登录的 Codex App Server，开发板使用离线中文 TTS 播报。
- 电脑关机后不要求继续产生新的 Codex 状态；设备只保留本地动画、时钟和最后快照。
- BLE 承担状态、Pet 选择和审批等轻量消息；Pet 大资源、PCM 与 JPEG 需要 USB 或同一局域网 Wi-Fi。
- Secure Boot、Flash Encryption、签名升级、法规认证和 72 小时实机长稳属于产品化验证，不伪装成当前 MVP 已完成。

## 2026-07-28 权威证据

### 已有真机证据

- Tab5 `tab5-e20af6e8` 上报板型 `m5stack-tab5-k145`、固件 `0.2.0`、协议 v4，兼容性检查通过。
- USB 与 BLE 同时建立认证加密会话，USB 为主链；拔线后 BLE 曾继续同步真实任务状态。
- 设备上报 `voiceDataReady=true`、`storageReady=true`，中文语音分区和已插入的 microSD 当前可用。
- 屏幕、颜色、触摸坐标、输入去抖、闪屏、Pet 动画和 Pet 切换回弹均已由用户逐项复验并确认。
- 当前任务、最近任务时间排序、线程 Token 与今日 Token 已使用真实 App Server 数据；`npm run test:live-tab5` 在真机状态中验证任务倒序和整数统计。
- 内置 `chibi-skadi` 正常播放，但它来自固件 SPIFFS，不作为 microSD 自定义 Pet 传输的物理证据。

### 已有软件与工具链证据

- ChatGPT 登录下的无 Key 链路已完成真实软件闭环：本地中文音频转写后，Codex App Server 返回宠物回复。
- 语音命令需要设备二次确认，未确认时不会创建可写 Codex 任务。
- 摄像头使用硬件 JPEG、加密分块、大小与 SHA-256 校验；图片以 `0600` 权限写入随机临时目录，分析后递归删除。
- 虚拟 Tab5 完成一次性配对、双链路加密、最大 V2 Pet 安装、审批和 Wi-Fi 接管。
- 固定种子压力测试完成 501 个认证会话、500 次链路切换、250 次资源中断恢复及协议故障注入。
- 完整回归共 150/150 项 JavaScript 测试通过，原生固件核心检查通过；真实 App Server 冒烟和 `doctor` 诊断均为 `ok`。
- v0.2.0 工厂镜像为 16,711,680 Bytes，SHA-256 `d5913ec318052ee61261f92a71dbd6498644cd89c34957563d00c79a294a55f1`。

## 当前逐项结论

| 项目 | 当前结论 | 关闭条件 |
| --- | --- | --- |
| 屏幕、触摸、颜色、动画与滚动 | 真机完成 | 已关闭 |
| 任务状态、最近排序、当前/今日 Token | 真机数据与自动化完成 | 已关闭 |
| 审批允许/拒绝 | 协议、Hooks 和虚拟闭环完成 | 下一次真实审批可作为持续回归，不阻塞 MVP |
| Pet 电脑/设备切换 | 真机完成 | 已关闭 |
| 自定义 Pet 源制作 | 电脑端目录、v1/v2 校验与转换完成 | 已关闭 |
| microSD 自定义 Pet 安装 | 软件完成，真机未取证 | 传输一个非 `chibi-skadi` 的 Pet，重启后仍可播放 |
| 无 Key AI 对话与命令 | 软件真实闭环完成，真机录放待取证 | 真机完成一次对话并听到回复；命令转写后出现二次确认 |
| 摄像头与 CV | 软件和固件完成，真机画面待取证 | 真机拍照后得到非空图片尺寸、字节数与回复 |
| USB | 真机基础链路完成 | 长期回归继续记录，不阻塞 MVP |
| BLE 无线 | 真机状态同步完成 | 已满足拔线后的轻量状态与控制 |
| Wi-Fi 高带宽无线 | 软件完成，当前公司网络环境不可验 | 在无门户认证的 2.4 GHz 局域网让电脑和 Tab5 同网复验 |
| 扬声器、电池与温升 | 能力与遥测可用，物理曲线未完成 | 实听音量；产品阶段记录电池/充电/温度 |
| 量产稳定与安全 | 设计基线完成，生产验证未完成 | 量产密钥、不可逆 eFuse 演练、法规与 72 小时实机测试 |

## 收尾顺序

1. 使用 `npm run test:live-tab5 -- --wait-for voice,vision` 捕获一次真机语音与拍照证据；报告只记录长度、尺寸和状态，不输出对话或图片内容。
2. 传输一个非固件内置 ID 的自定义 Pet，确认 microSD 安装、切换和重启保持。
3. 到可用的普通 2.4 GHz 网络后，以 `CODEX_DESK_DEVICE_HOST=0.0.0.0` 启动 Bridge，完成真实 Wi-Fi 拔线、路由器重启和恢复测试。
4. 运行完整回归、更新验收矩阵、创建中文提交并推送。
5. MVP 关闭后再单独进入 72 小时、弱信号、电源、温升、生产签名与认证阶段。

## 完成判定

功能 MVP 只有在语音、拍照和非内置自定义 Pet 三项真机证据补齐后才标记完成。公司网络不支持普通局域网设备接入，因此 Wi-Fi 射频验收必须保留为环境依赖项；这不改变 BLE 已能无 USB 同步轻量状态的事实，也不能被软件模拟结果冒充。
