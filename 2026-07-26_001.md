# Tab5 离线验收与真机收尾计划

## 当前目标

用户暂时无法接触公司内的 Tab5。此阶段不降低正式设备配对、认证或审批安全要求，先完成所有不依赖实体板的验证；浏览器 Tab5 模拟器和内存传输设备会话代替真机执行软件闭环。用户回公司后只执行剩余物理验收。

## 当前权威状态

- 目标硬件是 M5Stack Tab5 Kit `K145`，不是早期文档中的 CoreS3 K128。
- Tab5 已完成首次整机烧录、USB 枚举、屏幕显示和触摸硬件诊断。
- 正式配对键盘已修复释放事件与整屏高频刷新问题，最新固件已刷入；最终手感仍需用户回公司确认。
- 中文语音分区已烧录并通过固件完整性校验；音质、音量和温升仍需实听。
- 正式设备继续要求一次性 USB 配对码、每设备密钥、双向认证和加密会话。离线测试只使用临时目录和内存传输，不向正式运行路径加入免认证入口。

## 无真机阶段必须关闭的事项

1. 完整 JavaScript、HTTP、协议、配对、审批、Pet 和原生 C++ 固件核心测试。
2. 固定种子的 USB/Wi-Fi 切换、丢包、重复、乱序、ACK 丢失和 Pet 中断恢复压力测试。
3. ESP32-P4 真实工具链构建和完整工厂镜像生成；逐组件与整机哈希复验。
4. 真实 Codex App Server 冒烟，确认当前客户端仍提供项目依赖的接口。
5. Mock 控制面板视觉与交互验收：1280×720 设备布局、9 个动画、16 个看向方向、Pet 切换和审批弹层。
6. 更新验收文档，区分已经完成的真机事实、当前离线证据和仍需用户回公司执行的步骤。
7. 将阶段提交推送到用户指定仓库。

## 真机回来后保留的唯一验收

1. 输入新配对码，确认数字按下立即响应、命中准确并成功配对。
2. 配置 2.4 GHz Wi-Fi，拔掉 USB 后验证无线同步、路由器重启和电脑睡眠恢复。
3. 检查 microSD、自定义 Pet 实际传输/切换及随机断电恢复。
4. 实听中文语音与提示音，检查 RTC、电池、充电状态和温升。
5. 连续 72 小时运行，并记录内存、断线恢复、充电和机身温度。

## 完成判定

只有无真机验证全部通过、发布包重新生成并推送，且真机清单明确到可直接操作时，当前离线阶段才算完成。整个产品目标仍需用户回公司完成上述五项物理验收后才能关闭。
