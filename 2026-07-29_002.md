# 真机语音与摄像头验收

## 目标

1. 修复 Tab5 连续语音分片被误判为重复消息的问题。
2. 明确语音按钮的录音中状态，完成对话与命令真机验收。
3. 完成摄像头拍照、方向、色彩和 CV 回复真机验收。
4. 保留现有配对、SD 卡和用户数据，不执行擦除刷写。

## 验收标准

- 对话录音可持续到用户再次点击停止，完整语音能转成文字并得到宠物回复。
- 命令录音可生成待确认命令，用户确认后能执行并返回结果。
- 拍照可上传有效 JPEG，方向和颜色正确，CV 回复与画面内容一致。
- 自动化测试、固件检查、真机连接检查全部通过。

## 真机故障修复

- 语音按钮必须隔离同一次按压产生的重复触摸事件，不能刚开始就停止。
- 单个音频分片发送失败时继续录音；只有连续失败才结束并显示具体原因。
- USB 报文末尾换行与正文采用相同的限时重试，避免瞬时背压中断语音。
